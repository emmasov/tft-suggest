import * as React from "react";
import { ItemComponentId } from "utils/items";

interface Props {
  availableItemComponents: ItemComponentId[];
}

const ItemPossibilityGrd: React.FC<Props> = ({}) => {
  return <div></div>;
};

export default ItemPossibilityGrd;
